[project]
name = "geography-quiz"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "geopandas>=1.1.4",
    "pandas>=3.0.5",
    "pydantic>=2.13.4",
]

[tool.uv]
package = true

[tool.setuptools.packages.find]
where = ["src"]
